# Sandbox Reference

Legacy prototype files are mirrored here for handoff traceability:

- `main.py`: websocket translation prototype.
- `server.py`: token/static prototype server.
- `pipeline.py`: early LiveKit worker prototype.

These files are references only and are not the primary V1 runtime path.
